/**
 * SUPERSEDED — kept only as a pointer.
 *
 * Projects now live in the `projects` table, managed at /admin/gallery.
 *
 * The "no fabricated content" rule still holds and is now enforced by the
 * schema rather than by an empty array: `published` defaults to FALSE, so
 * nothing reaches the public site until someone deliberately ticks it.
 */
export {};
